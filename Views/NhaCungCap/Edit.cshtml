@model Testthi.Models.NhaCungCap

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Nhà Cung Cấp </h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MaNhaCungCap" />
            <div class="form-group">
                <label asp-for="MaNhaCungCap" class="control-label">Mã nhà cung cấp</label>
                <input asp-for="MaNhaCungCap" class="form-control" />
                <span asp-validation-for="MaNhaCungCap" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TenNhaCungCap" class="control-label">Tên nhà cung cấp </label>
                <input asp-for="TenNhaCungCap" class="form-control" />
                <span asp-validation-for="TenNhaCungCap" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
